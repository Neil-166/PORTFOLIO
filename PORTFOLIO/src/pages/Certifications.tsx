import { FaArrowDown, FaAward, FaCertificate } from 'react-icons/fa6';
import { Seo } from '@/components/ui/Seo';
import { PageShell } from '@/components/ui/PageShell';
import { Section } from '@/components/ui/Section';
import { EmptyState } from '@/components/ui/EmptyState';

export default function Certifications() { return <><Seo title="Certifications" path="/certifications" /><PageShell><Section eyebrow="CREDENTIALS" title="Proof of practice, arriving soon."><EmptyState title="Certificate gallery in progress" copy="As coursework and external learning milestones are completed, verified certificates and download links will live here." action={<span className="inline-flex items-center gap-2 text-sm font-bold text-brand"><FaCertificate />Curating milestones</span>} /></Section><Section eyebrow="TIMELINE" title="The next credentials."><div className="grid gap-4 md:grid-cols-3">{['Core programming', 'Full-stack foundations', 'Cloud & deployment'].map((item, index) => <div key={item} className="rounded-2xl border border-dashed border-line p-6"><span className="grid h-10 w-10 place-items-center rounded-xl bg-brand/10 text-brand">{index === 0 ? <FaAward /> : <FaArrowDown />}</span><p className="mt-5 text-xs font-bold tracking-wider text-muted">UP NEXT</p><h2 className="mt-2 font-display text-lg font-bold text-ink">{item}</h2><p className="mt-2 text-sm leading-6 text-muted">Completion evidence and a direct download will appear here.</p><button disabled className="mt-5 inline-flex items-center gap-2 rounded-lg border border-line px-3 py-2 text-xs font-bold text-muted"><FaArrowDown />Download when earned</button></div>)}</div></Section></PageShell></>; }
